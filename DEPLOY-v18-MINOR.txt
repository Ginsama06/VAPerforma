Deploy VA Performa Website v18 Minor Revision

Copy this folder into:
  C:\Users\Gin\Desktop\l6\VAPerforma

Then run:
  Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
  npm install
  npm run check
  git add .
  git commit -m "Update Why VA Performa company description"
  git push origin main
